# 2024 CUMCM 本科组 A/B/C 题 · 深度解析（技能参考手册）

> **使用说明**：本手册按「可照着做」原则编写。所有**具体数值以官方题目 PDF 与附件为准**；凡我未能 100% 核实的具体数字一律标注 **「待核实」**，绝不编造。公式与建模思路为真题通用、可直接实现。

---

## 2024 A 题：「板凳龙」闹元宵（螺线盘入 · 碰撞终止 · 最小螺距 · S 形调头 · 速度上限）

> **本章已按一次完整复算全面修订**（原版把盘入方向、Q3/Q4 小问口径写错了，会误导求解）。
> 下列"实测答案"均为该项目复算所得（可复现），口径以当年官方题面为准。

### 五问真实口径（**已核实**）

| 问 | 要求 | 交付 |
|---|---|---|
| **1** | 螺距 55 cm 螺线**顺时针盘入**，各把手在螺线上，龙头前把手恒 1 m/s，初始在第 16 圈 A 点 | 0–300 s **每秒** 224 个把手的位置与速度 → `result1.xlsx` |
| **2** | 求**盘入终止时刻**（板凳开始相碰，不能再盘入）及该时刻各把手位置/速度 | `result2.xlsx` |
| **3** | 调头空间=圆心 Φ9 m（r=4.5 m）；求**最小螺距**使龙头前把手能盘入到调头空间边界 | 论文数值 |
| **4** | 盘入螺距 1.7 m；盘出螺线与之**关于中心对称**；调头路径=**两段相切圆弧的 S 形**（前弧半径=后弧的 2 倍，与盘入/盘出螺线均相切）；问**能否调整圆弧使调头曲线更短**；t=0 为调头开始 | −100 s…100 s 每秒位置/速度 → `result4.xlsx` |
| **5** | 沿问题 4 路径，龙头速度恒定，求**龙头最大速度**使**所有把手速度 ≤ 2 m/s** | 论文数值 |

### 已核实的几何事实（照抄可省 1 小时）

- **223 节**：龙头 1 节板长 **341 cm**，龙身 221 节 + 龙尾 1 节均 **220 cm**；板宽 30 cm；
  每板两孔、孔心距最近板端 27.5 cm ⇒ **龙头板孔距 286 cm、其余 165 cm**。
- **把手 224 个**：h=0 龙头前把手；h=1..221 = 第 1..221 节龙身前把手；h=222 龙尾前；h=223 龙尾后。
  全链"孔到孔"总长 = 2.86 + 222×1.65 = **369.16 m**。
- 等距螺线（阿基米德）$\rho=b\theta,\ b=p/2\pi$；龙头初始**第 16 圈** $\theta_0=32\pi$
  （$r_0=16p$：p=0.55 时 8.8 m、p=1.7 时 27.2 m，A 落在 +x 轴）。
- **方向（原版写反了，务必按此）**：龙头在**最内**（第 16 圈）、**龙身/龙尾在外侧拖尾**（θ 递增向外）；
  龙头向圆心内缩（θ 减小）。**判据**：问题 3 要求龙头盘入到**圆心** r=4.5 m 边界 ⇒ 身体必须在外侧；
  若身体在内侧，龙头内缩会钻进自身身体、立即碰撞，模型自相矛盾。
- **速度模型**：用**局部刚杆约束递归** $s_i = s_{i-1}\dfrac{\hat t_{i-1}\cdot \vec L}{\hat t_i\cdot \vec L}$
  （龙头取 1 m/s）。**不要**用"龙头角 ±ε 的全局链式差分"：223 层嵌套求根会把噪声放大，
  实测深层把手会算出 1.07–1.67 m/s 的**假速度**。

### 逐问要点与**实测答案**

#### 问 1（螺线弧长参数化 + 逐节弦长定位）

- 龙头：弧长 $s(t)=S(\theta_0)-vt$ 反解 $\theta(t)$，$S(\theta)=\frac b2[\theta\sqrt{1+\theta^2}+\operatorname{asinh}\theta]$。
- 龙身/龙尾：**刚性弦**约束 $|\mathbf P(\theta_i)-\mathbf P(\theta_{i-1})|=L_i$（龙头 2.86 m、其余 1.65 m），
  沿 θ **递增**方向逐节二分求根。
- **实测**：弦长模型 vs 弧长近似 的位置偏差 **max 0.403 m / 平均 0.226 m** ⇒ 必须用弦长模型（并作为误差分析素材）。
- **实测速度分布**：全部 ∈ **[0.9965, 1.0000]**（龙头恒 1 m/s、越靠尾越慢）——
  **不是"所有把手都等于 1"**，但也**都不是 >1**（与问 4/5 的调头段形成鲜明对比）。
- 0 s / 300 s 龙头位置：(8.800000, 0) → (4.420274, 2.320429)。

#### 问 2（碰撞判定的终止时刻）

- **判据（推荐）**：每块板取**全长线段**（两孔位置各向板端外延 27.5 cm），
  对**任意两不相邻板**（|i−j|≥2，排除共享把手的连接板）求**线段—线段最短距离**，
  **首次 < 0.30 m（板宽）**即碰撞。
  弃用判据：只比把手点距离（漏板身相碰）；只看相邻圈径向间距（螺距 55 cm > 板宽 30 cm，
  会误判"永不碰撞"）。
- **实测**：$t_{\rm end}=\mathbf{401.30742\ s}$（p=0.55 m），该刻最近板距 0.300000 m，
  龙头位于 (1.908143, −1.884418)、r=2.68 m；**支配板对 = 龙头板（3.41 m 最长） vs 第 20 块板**。
- 必做检验：$t_{\rm end}\pm\varepsilon$ 两侧"不碰/碰"，证明是临界点。

#### 问 3（最小螺距）

- **判据必须是"全程首个碰撞半径 ≤ 4.5 m"**：龙头从起点向内盘，**首次** min_dist<0.30 m 的半径。
  只查"到达 r=4.5 那一刻不碰"是**必要不充分**，会低估（实测会把 0.46 误算成 0.4443）。
- **光滑判据优于跳变判据**：$C_{\min}(p)=\min_{r\in[4.5,\,16p]} C(p,r)$ 连续单调，求 $C_{\min}=0.30$ 良态；
  而"首次碰撞半径"在最近板对切换处**跳变**，对它插值/二分必偏（实测 0.46048 vs 0.46060）。
- **实测答案：最小螺距 $p^*\approx\mathbf{0.4606\ m}$**（临界区间 [0.4604, 0.4606]；论文取 0.46 m）。
- **无量纲结论**：以板宽 w 为尺度，$p^*\approx\mathbf{1.535\,w}$，且 $p^*\propto w$ 严格成立
  （w=0.25/0.30/0.40 m 下 $p^*/w$ 偏差 0）。
- 鲁棒区间 **[0.4335, 0.4891]**，由"板端外延"假设主导；**起始圈数 16 vs 16.5 完全无影响**。
- 机制：支配板对 (0, 19)，临界接触仅深 **0.6 mm**（谷底 r≈4.5565），故第 3 位小数对模型细节敏感。

#### 问 4（S 形调头 + "能否更短"）

- **S 曲线是确定解**（实测参数，p=1.7 m）：$A=(-2.711856,-3.591078)$、$B=-A$、$|AB|=9.000000$（**对径点**）；
  **两端切向严格相同**（中心对称的必然：$t_B=t_A$，$t_A\cdot t_B=1.000000000000$，且与弦夹角 86.56°≈垂直）；
  $R_1=3.005418$、$R_2=1.502709$（比值精确 2）、切点 $C=(0.903952,1.197026)$；
  两弧扫掠角**均 173.1184°**（不是 180°）；**调头段弧长 $L_S=13.621245$ m**，曲线最大半径恰 4.5000。
- **复合路径**：$s<0$ 在盘入螺线、$0\le s\le L_S$ 在 S 曲线、$s>L_S$ 在盘出螺线；
  龙头 1 m/s ⇒ $s_{\text{head}}(t)=t$ ⇒ **调头段只占 $t\in[0,13.62]$ s**。
- **后半问答案是"看解释"的两段式结论（务必两个都写，别只写一个）**：
  1. **若"调整圆弧"= 在两段相切圆弧族内调比例**（端点 A、B 固定在 r=4.5、两端切向固定）⇒ **不能变短**。
     因为 $L\equiv(R_1{+}R_2)\cdot 2\theta=\dfrac{|AB|\,\theta}{\sin\theta}=\mathbf{13.6212449\ m}$，
     而 $R_1{+}R_2=|AB|/(2\sin\theta)$ 与每弧扫掠角**都与 $R_1{:}R_2$ 无关**（可解析证明：
     $R_1{+}R_2=D/(2\,u\!\cdot\!n)$、单位切点向量 $\hat e=2(u\!\cdot\!n)u-n$ 均与比例无关）
     ⇒ 调比例只把弧长在两条弧间搬运、改变形状与最大曲率，**长度严格不变**（裕度 0.0000%）。
  2. **若允许改变曲线形式**（仍与两螺线相切、仍在 r≤4.5 内，只约束**曲率半径下限 $R_{\min}$**）⇒ **能变短**：
     离散化数值优化给出最优解是"**近直线穿越圆盘 + 两端各一个小半径急转**"（曲率**不是**分段常数 ⇒ 不是两段圆弧），
     同档比较（取 $R_{\min}=1.502709$ m，即题给解自身的曲率下限）**$L_\infty\approx\mathbf{11.3456}$ m，比 13.6212 m 短 16.8%**；
     $R_{\min}=1.0$ → 10.3427 m（−24.1%）；$R_{\min}=0.8$ → 10.0495 m（−26.3%）；
     $R_{\min}\to0$（允许任意尖折角）时下确界为弦长 $|AB|=\mathbf{9.000000}$ m（−33.9%）。
     **注意 $R_{\min}$ 是题目没给的量**，所以"两段圆弧的相对优劣完全取决于这个未给定的转向半径下限"——
     论文里应当把这句话写出来，这本身就是一条"模型假设敏感性"结论。
- **题意边界（也建议写进论文）**：若把"调整圆弧"进一步理解为**允许切点沿螺线内移**（不再固定在 r=4.5），
  问题会变得**不适定**——存在一族"总转向 → 0、几乎贴着调头空间外缘"的退化曲线，长度 → 0
  （数值上可压到 0.5 m 以下，但那已不是"调头"）。故切点应固定在 r=4.5 的 A、B。
- **陷阱**：绝不能把"调头空间半径 4.5 m"当作**曲率半径下界**去套 $L\ge\rho\!\int|\kappa|ds$
  ——会得到 **27.19 m 的假下界**（大于真值 13.62 m，自相矛盾）。该式只在 $|\kappa|\le1/\rho$ 处处成立时有效。
- **陷阱（几何判据）**："圆弧是否落在 r≤4.5 内"**不能用 $|O|+R\le R_{\text{turn}}$ 粗判**。
  实测题给解有 $|O_1|+R_1=4.516224>4.5$，但它的**最远点落在该弧段未覆盖的角度上**，
  弧段实际整体在空间内 ⇒ 用粗判会**直接把正确解误杀**。必须用「弧段覆盖判据」：只在弧的真实扫掠区间上采样/取极值。
- **两条解分支别混淆**：方程组有两族解——
  ① **高转向分支**（总转向恒 346.2369° = 2×173.1184°）：题给解属此族，$L\equiv13.621244907$ m（72 组半径比扫描极差 1.3e−9 m）；
  ② **半转向分支**（总转向恰 180°）：$L\in[13.6545,14.1372]$ m，比题给解**更长**（题给比该族最短还短 0.2445%）。
  对"不变性"下结论时必须指定分支，否则会把两族混在一起算出"R1+R2 随比例变化"的假象。
- **解算细节**：该残差方程对切点极角 $\varphi$ 常呈**重根**（只相切于 0 而不变号），
  `brentq` 找不到根，必须改用**最小化 $|\text{resid}|$**；另有若干隐蔽 off-by-one/符号坑（`h2` 应随 $R_1$ 闭式求、
  旋转前先单位化、采样型 $\max|P|$ 在临界处会溢出到 $4.5+\varepsilon$）。

#### 问 5（龙头速度上限）

- 原理：把手速度与龙头速度**成正比**（刚杆约束线性）⇒ $V_{\max}=2\Big/\max_{s,i}\dfrac{v_i(s)}{V_{\text{head}}}$。
- **实测**：速度比峰值 $\approx\mathbf{1.6048}$，出现在 **s≈14.48 m（龙头刚驶出调头段）**，
  支配把手 h≈3–7——它们**正压在 S 形调头段上**（R₂=1.5 m 高曲率），而龙头已在低曲率盘出螺线上。
  ⇒ **$V_{\max}\approx\mathbf{1.246\ m/s}$**。
- **必须加密扫描**：该峰宽仅约 1 m，**粗扫 step=2 m 会完全漏掉**（只看到 1.4222，答案会错成 1.406）。
  做法：变步长扫描（高风险区 0.05、其余 0.25）+ 峰附近精化 0.001 + 双侧验证（$V_{\max}$ 处 ≤2、$V_{\max}(1{+}10^{-9})$ 处 >2）。
- **不能直接读 `result4.xlsx` 求最大值**：该文件按 **1 s 采样**（t = −100…100 整数秒），而峰值在 **t ≈ 14.48 s**，
  正好落在 14 s 与 15 s 两个采样点之间 ⇒ 文件里的最大速度只有 1.4222 m/s。
  **Q5 必须沿弧长连续扫描重新求峰值**——这是"结果文件给的是采样值、不是极值"的典型陷阱。
- 分段特征（$V_{\text{head}}$=1 m/s）：盘入段 [0.9939, 1.0000]；**调头段 [0.5404, 1.4222]**；盘出段 [0.9031, 1.4191]。

### 交付物格式（**本次真实翻车点，务必先做**）

- 模板 `resultN.xlsx`：**第 1 行 = 时间表头、第 1 列 = 把手标签，数据从 (行2, 列2) 开始**。
  - 位置表：449 行 × 302 列，把手 h → **x 行 = 2+2h、y 行 = 3+2h**；
  - 速度表：225 行，h → **行 = 2+h**；result2.xlsx（Sheet1）：225 行 × 4 列，h → **行 = 2+h、列 2/3/4**。
- 写错行/列会**覆盖表头与标签、整体错位一格、末行/末列留空**——数值全对但结构全错，
  按标签读数会把对象整体错位一个。
- **动手前先跑** `scripts/xlsx_deliverable.py probe 附件/resultN.xlsx` 看行列契约；
  填完跑 `check` 体检（会抓出"表头被覆盖 / 标签被数值覆盖 / 末行空"等）。

### 求解算法与 Python 要点

| 步骤 | 方法 | 库/函数 |
|---|---|---|
| 螺线 $s\leftrightarrow\theta$ 互转 | 解析弧长 + 单调求根 | `scipy.optimize.brentq`；$S(\theta)=\frac b2[\theta\sqrt{1+\theta^2}+\operatorname{asinh}\theta]$ |
| 逐把手弦长定位 | 一维求根（单调） | `brentq`（螺线用 θ，复合路径用**弧长 s**） |
| 位置→速度 | **局部刚杆递归**（勿用全局差分） | 自写向量递推 |
| 碰撞判定 | 全板线段 + 线段-线段最短距离 | 手写/向量化；**候选对可剪枝**（见下） |
| 两段圆弧 S 曲线 | 双圆弧(biarc)相切条件 | 一维求根；或 `least_squares` 解 8 元方程组 |
| 交付 Excel | **先 probe 模板再按行位填** | `openpyxl`；配合 `scripts/xlsx_deliverable.py` |
| 可视化/核对 | 螺线 + S 曲线 + 逐帧龙形 | `matplotlib`（等比例 `set_aspect('equal')`） |

**性能经验（实测）**：碰撞判定的瓶颈是**候选对数量**而非链条求解——
把"全量 9653 对"剪枝成"龙头板 vs 其余 k∈[2,45]（44 对）**逐位相等**、纯板距部分快 **7.25×**；
单配置从 17 ms 降到 0.67 ms。**"给链条求解写牛顿法提速"是伪需求**（实测错误实现差到 112 rad）。

### 模型检验与灵敏度（必做）

1. **弦长 vs 弧长**双模型对照（本题 max 0.403 m ⇒ 说明为何必须用弦长）。
2. **速度双算法互验**：局部刚杆递归 vs 中心差分数值微分（实测一致到 1e-9）。
3. **碰撞/终止临界**：$\pm\varepsilon$ 双侧"碰/不碰"；并给出支配板对与临界相切放大图。
4. **判据敏感性**：Q3 要报告"端点快照判据会低估多少"（0.4443 vs 0.4606）；
   Q5 要报告"扫描步长是否会漏峰"（step 2 漏成 1.4222，实际 1.6048）。
5. **交付物格式检验**：行列位、位数（6 位小数）、末行/末列非空——**用独立脚本卡**，不要靠肉眼。

### 国一亮点建议

1. **把"弦长 ≠ 弧长"做成定量误差分析**（0.403 m），而不是一句"我们用弦长"。
2. **碰撞用线段—线段最短距离 + 临界相切放大图**（问 2），并给出支配板对。
3. **证明式结论优于数值结论**：问 4 的"不能变短"给出恒等式 $L=|AB|\theta/\sin\theta$ 与唯一性论证；
   问 3 给出无量纲标度律 $p^*\approx1.535w$——都比"我们算出来是 x"高一个层次。
4. **调头段是速度放大器**：龙头恒 1 m/s 时把手最高 1.60 m/s，这一句话把问 4 与问 5 串起来。

### 高频失分点

1. **盘入方向搞反**（龙头在最内还是最外）——用问题 3 的"盘入到圆心边界"当判据即可自检。
2. **把板凳当"等弧长"等分**：系统性错位（本题 max 0.403 m）而不自知。
3. **速度用全局差分**：深层把手算出假速度（实测 1.07–1.67 m/s）。
4. **只比把手点距离判碰撞**，或误用"相邻圈径向间距"（螺距 > 板宽 ⇒ 误判永不碰撞）。
5. **端点快照当判据**（Q3 低估 3.5%）；**粗扫描漏窄峰**（Q5 漏成 1.406 vs 1.246）。
6. **xlsx 行列位写错**：数值全对、结构全错，且自检发现不了（自检用了同一套错约定）。

---

## 2024 B 题：电子产品生产过程中的质量决策（检测 / 次品 / 拆解）

### 交付目标

- **一句话总目标**：把「是否检测零配件、是否检测（半）成品、不合格品是否拆解/回收」建模为**期望成本（或利润）最小的 0-1 决策**，并对给定情形给出决策方案与量化指标；高阶问引入**抽样检验与统计不确定性**。
- **逐小问交付**（口径以官方为准，核心逻辑如下）：
  - **问 1**：对给出的 **6 种情形**（参数：零配件 1/2 次品率、单价、检测成本、装配成本、成品售价、成品检测成本、拆解成本、调换（退回）损失等；具体数值**待核实/官方表**），分别给出「零配件 1/2 是否检测、成品是否检测、不合格品是否拆解」的最优方案及对应指标（期望成本/利润）。
  - **问 2**：推广到 **$m$ 个零配件 + $n$ 道工序**（多级半成品/成品）的流水线，建立一般决策模型并给出方案。
  - **问 3**：检测方式引入**抽样检验**，设计满足给定显著性水平/置信度（已核实官方思路用约 **95% 信度**，具体 α/β 待核实）的最小检测量抽样方案，并与决策联立。
  - **问 4**：回到问 1 的 6 种情形，假设**零配件/成品次品率未知、需抽样估计**，设计抽样方案并重新给出最优决策。

### 问题拆解与建模思路

**统一工具：期望成本分解**。设：
- 零配件 $i$：次品率 $p_i$、单价 $c_i$、检测成本 $d_i$。
- 装配成本 $A$；装配后成品为次品的概率 $p_{\rm asm}$（由各零配件是否次品 + 装配工序自身引入次品率决定，例如「任一关键零配件为次品 → 成品次品」或组合逻辑，**具体依赖关系以官方为准**）。
- 成品售价 $P$、成品检测成本 $D$、不合格品退回/调换损失 $W$（客户退回再损失）、拆解成本 $R$、拆解后可回收价值 $S$。

决策变量（0-1）：$x_i$（是否检测零配件 $i$）、$y$（是否检测成品）、$z$（不合格成品是否拆解）、$w$（是否回收/报废）。

#### 问 1：本质是「枚举/阈值比较的确定性决策」

- 对 2 个零配件，决策空间仅 $2^3=8$（×拆解选项），可直接**全枚举**取期望成本最小；更重要的是给出**解析阈值**：
  - 检测零配件 $i$ 的收益 = 不检测时次品流入下游导致的期望损失 $E_i$（下游整改成本/成品损失）与检测成本 $d_i$ 比较 → 当 $p_i E_i \ge d_i$ 时检测。
  - 拆解 vs 直接退回：比较「拆解回收净值」$S-R$ 与「调换损失」$-W$。
- 期望成本（示意，需按官方成本项补全）：
$$C=\sum_i (c_i + x_id_i) + A + yD + (1\!-\!y)\text{[次品外流损失]} + z\,(R-S)\cdot \Pr(\text{检出次品}) + \ldots$$

#### 问 2：本质是「多阶段决策树 / 动态规划 / 等价整数规划」

- 状态 = 各零配件/半成品是否已检测；阶段 = 各装配工序。次品向下游**传播**，每道工序可执行「不检测 / 全检」。
- 建模为**决策树**逐层算期望成本，或写成**0-1 整数规划**（目标线性、约束为「每级只能选一种检测策略」），直接交给 MILP 求解器。

#### 问 3：本质是「抽样验收（OC 曲线）设计」

- 抽样方案 $(n,c)$：抽 $n$ 件，次品数 $X$，若 $X\le c$ 则接收。**OC 曲线**
$$L(p)=\Pr(\text{接收}\mid p)=\sum_{k=0}^{c}\binom nk p^k(1-p)^{n-k}.$$
- 要求：在**合格质量水平 AQL $p_0$** 处 $L(p_0)\ge1-\alpha$（$\alpha$ 生产者风险），在**极限质量 LTPD $p_1$** 处 $L(p_1)\le\beta$（$\beta$ 消费者风险）。求**最小 $n$ 及对应 $c$**（$n$ 小时用二项分布精确算，勿盲目用正态近似）。

#### 问 4：本质是「抽样估计 + 决策闭环」

- 先用题给显著性/置信度反推（$n,c$）得到次品率**区间估计/点估计**，再把这些估计代入问 1 的期望成本模型，使「检测不确定性」进入决策；报告抽样量与期望成本的关系。

### 求解算法与 Python 要点

| 步骤 | 方法 | 库/函数 |
|---|---|---|
| 问 1 枚举 | 笛卡尔积全枚举 | `itertools.product([0,1], repeat=k)` |
| 问 2 决策树/DP | 手写期望递归 或 图遍历 | `networkx`（可选）；或直接 MILP |
| 整数规划 | 0-1 线性规划 | `scipy.optimize.milp`（≥1.9）或 `pulp`/`ortools`（推荐 PuLP+CBC，变量可命名） |
| 抽样 OC 曲线 | 二项分布 CDF | `scipy.stats.binom.cdf(c,n,p)`、`.sf`；小 $p$ 可用 `poisson` |
| 反解 $(n,c)$ | 枚举 $n,c$ 找满足 α/β 最小 $n$ | 双重循环 + `binom.cdf`；或 `scipy.optimize` |
| 置信区间 | 抽样比例区间 | `scipy.stats.binomtest` / `proportion_confint`（`statsmodels.stats.proportion`） |
| 蒙特卡洛验证 | 过程仿真 | `numpy.random` 按次品率逐级抽样，统计期望成本 |

**与代码模板关系**：问 1 直接枚举（8 种方案）即可；问 2 用 PuLP 写 0-1 MILP 最省事、可扩展；问 3/4 的关键是**正确用 `binom.cdf` 构造 OC 曲线**，而不是抄错 α/β 不等式方向。

### 模型检验与灵敏度（必做）

1. **期望成本分解核对**：拆出「采购 / 检测 / 装配 / 成品检测 / 拆解 / 调换损失」六项，验证加总 = 总期望；把每种决策组合的表格列出，说明最优方案是全局而非局部。
2. **决策断点分析**：画「检测与否」随 $p_i$（次品率）与 $d_i$（检测成本）变化的**相图/断点**，说明阈值 $p_iE_i=d_i$ 两侧决策翻转。
3. **抽样方案自洽**：用蒙特卡洛在 AQL/LTPD 处抽样，验证实际风险 ≈ α、β。
4. **问 4 闭环**：抽样样本量 $n$ 变化 → 次品率估计误差 → 期望成本变化，报告「检测投入 vs 决策不确定性」的权衡曲线。

### 国一亮点建议

1. **解析阈值 + 枚举双轨呈现**：既给出「边际比较」的解析判据（可解释性强），又用枚举/求解器验证，体现模型不是「暴力算出来的黑箱」。
2. **OC 曲线与 α/β 精确、图可视化**：明确使用二项/超几何而非正态近似，画出 OC 曲线与 (n,c) 选取过程；问题 4 做「抽样—决策」闭环。
3. **蒙特卡洛仿真与解析期望互证**：用随机仿真复现期望成本（附置信区间），增强说服力（已见国二解法采用仿真路线，可参考）。

### 高频失分点

1. **决策方向错误 / 漏成本项**：没算清「不检测 → 次品流入下游的更大损失」，或漏掉拆解回收价值、调换损失的概率加权、装配工序自身引入的次品。
2. **期望成本公式不闭环**：各项没有按「是否发生」的概率相乘（把调换损失当固定发生、或把拆解成本当无条件发生）。
3. **抽样检验方向/近似错误**：把 OC 曲线的 α、β 对应关系写反；$n$ 较小时用正态近似导致 $(n,c)$ 不可行或不满足风险要求。

---

## 2024 C 题：农场农作物种植策略（面积分配与收益/风险权衡）

### 交付目标

- **一句话总目标**：以「分类耕地 + 多作物 + 销量/价格/亩产约束（含轮作与土地利用规则）」建立种植面积优化，从单年线性规划 → 多年「预测 + 优化」→「风险/稳健优化」逐级深化。
- **逐小问交付**（口径以官方为准）：
  - **问 1**：在「**超出预期销量的超额产量滞销（打折/浪费）**」假设下，求 **2024 单年**最优种植面积分配与最大净收益（各作物、各土地类型分配明细）。
  - **问 2**：利用往年数据**预测 2025–2030 各年**销量/价格/亩产，逐年（或多年联动）给出 6 年最优方案，说明轮作等跨年约束的影响。
  - **问 3**：**放开假设**（价格波动、滞销处理、存储、替代作物等），引入**收益风险/稳健性**，给出 2024–2030 综合方案，并量化比参考情形（或问题 2）**收益提升/风险改善**的百分比。

### 问题拆解与建模思路

**数据要素**（具体值**待核实/官方附件**）：
- 土地类型：**平旱地、梯田、水浇地、大棚**（大棚分编号与类型，常见约 16 个，总面积约 1200 亩量级）；附件给出每块地的**类型与面积**。
- 作物：粮食（小麦、玉米、**大豆**）、**水稻**（限水浇地/大棚）、蔬菜若干（黄瓜/番茄/豆角等）、**食用菌**（限大棚）。
- 每作物参数：**亩产、种植成本、平均售价、预期销量**，以及**作物—土地适配矩阵**（哪些作物可在哪类地种）。附件另给往年的价格/销量/亩产（用于问题 2 预测）。

**核心决策变量**：$x_{c,f}$ = 作物 $c$ 在地块 $f$ 上的种植面积（亩）；问题 3 可能需要 0-1 变量 $y_{c,f}$（是否种植）以表达轮作/单一大棚整块约束。

#### 问 1：本质是「线性规划（分段线性收益处理滞销）」

- 目标：最大化净收益 $\sum_{c,f}(\text{销售收入}-\text{成本}\cdot\text{面积})$。
- 关键技巧——**滞销的分段线性化**：单块收益 = 正常价 $\times\min(\text{产量},\text{销量})$ + 折价 $\times\max(0,\text{产量}-\text{销量})$。引入**虚拟「废弃/折价量」$u_c\ge0$** 与约束 $\text{产量}_c + u_c = \text{销量}_c + \text{折价量}_c$ 等，写成纯线性（避免 $\min$ 非光滑）。
- 约束：
  - $\sum_c x_{c,f}\le A_f$（每块地面积）；
  - `适配矩阵`：不适配的 $c,f$ 取 $x_{c,f}=0$；
  - 各作物总种植面积 $\le$ 其可在用的土地上限（分类型汇总）。

#### 问 2：本质是「时间序列/回归预测 + 逐年联动优化（预测—优化闭环）」

- 对销量、价格、亩产做预测：数据点少 → 常用**线性趋势/指数平滑/Holt-Winters/简单 ARIMA**（`statsmodels`）；务必做**留出验证**并报告 MAPE/RMSE，不要只给外推线。
- 逐年优化并在年份间加入**轮作递推**：地块 $f$ 第 $t$ 年种什么，约束第 $t+1$ 年（如大豆种植后 2–3 年内不得重茬、水稻/蔬菜需轮作等，**规则以官方为准**）。

#### 问 3：本质是「风险建模：均值—方差/CVaR/鲁棒/多情景随机规划」

- 把价格、亩产、销量视为随机变量（可基于问题 2 预测分布生成**情景集**）。
- 目标改为「期望收益最大化，同时控制风险」：
  - **均值—方差**：$\max\ \mathbb E[Z]-\lambda\operatorname{Var}(Z)$（二次规划或线性化）；
  - **CVaR**（条件风险价值）：引入参数 $\alpha$ 与辅助变量，化为线性；
  - **鲁棒优化 / 多情景**：对最坏情景或 $\min\text{–}\max$ 目标做稳健解。
- 需**明确比较基线**：与问题 2（或附件参考情形）比收益提升 $\%$ 与风险下降 $\%$，并解释新增因素（存储延期销售、作物可替代、价格波动）如何改变分配。

### 求解算法与 Python 要点

| 步骤 | 方法 | 库/函数 |
|---|---|---|
| 问 1 LP | 线性规划 | `scipy.optimize.linprog`（标准形）或 **`pulp`（推荐，变量/约束可读）** |
| 问 1 滞销线性化 | 引入折价/废弃变量 | 手写约束；目标分段线性化 |
| 适配矩阵/土地分型 | 变量下界=0 + 约束 | 直接用字典批量加约束 |
| 问 2 预测 | 时间序列/回归 | `statsmodels`（`Holt`/`ExponentialSmoothing`/`ARIMA`）、`sklearn.linear_model`；交叉验证 |
| 问 2 逐年联动 | 多年 LP（递推） | `pulp` 循环年份建变量/约束 |
| 问 3 风险 | 均值—方差/CVaR/情景 | CVaR 线性化手写；或 `cvxpy`；蒙特卡洛情景生成 `numpy.random` |
| 大模型备选 | 遗传算法（一般不需要） | `deap`/`geatpy`（仅当规模大/非线性时） |

**与代码模板关系**：问 1 是经典 LP（PuLP+CBC 秒解）；问题 2 的难点不在求解器而在**预测合理性与轮作跨年约束是否写全**；问题 3 是体现区分度的关键，建议用 CVaR/多情景而非简单加一个方差惩罚。

### 模型检验与灵敏度（必做）

1. **约束完整性核对**：逐类土地（平旱地/梯田/水浇地/大棚）打印面积利用率，确认适配矩阵与「大豆适宜梯田、水稻限水浇地/大棚、食用菌限大棚」等**题目显式规则都落实**。
2. **滞销收益核对**：对某作物手工算一个「产量<销量」与「产量>销量」的收益，验证分段线性表达正确。
3. **预测—优化闭环**：报告预测误差（MAPE/RMSE）；做「预测值 ±5%/±10% 摄动下最优收益如何变」的敏感性，说明方案稳健性。
4. **风险/稳健性**：给出收益分布直方图、不同风险厌恶参数 $\lambda$/不同 CVaR 水平 $\alpha$ 下的方案变化（有效前沿），证明不是单点结论。

### 国一亮点建议

1. **滞销显式线性化并说明「折价比率」设定的依据**（不同假设下的分配对比），而不是含糊地截断。
2. **预测—优化闭环 + 多情景/鲁棒优化**：不只线性外推；给出「预测误差→收益损失」的定量关系与稳健方案；问题 1 与问题 3 的**收益提升百分比**要算清并解释来源。
3. **轮作/重茬用 0-1 变量严格建模**，给出逐年种植甘特图/热力图，直观展示「豆类轮作 + 不重茬」是如何被满足的。

### 高频失分点

1. **土地约束漏写/适配错误**：把总耕地当单一上限，忽略**分类土地**（每类有各自上限）与作物—土地适配矩阵，导致方案「水稻种在旱地」等不可行解。
2. **轮作/重茬未建模或跨年脱节**：问题 2/3 最易漏「前一年种植决定下一年约束」的递推耦合，只做独立单年。
3. **滞销收益算错 / 风险空谈**：把超额产量按全价计收益；风险部分只用定性文字没有可复算的 CVaR/方差指标。

---

## 2024 难度与选题提示

| 题 | 核心能力 | 上手难度 | 区分度所在 | 选题建议 |
|---|---|---|---|---|
| **A 板凳龙** | 微分几何/运动学 + 数值求根 + 几何碰撞 | **中高**（门槛最高） | 弦长 vs 弧长、碰撞精确判定、曲率受限调头 | 编程+几何强、能画动图的队选它上限最高；但要写深才有国一，浅做易平庸 |
| **B 质量决策** | 期望成本/决策树 + 抽样检验(OC 曲线) | **中**（模型最标准） | 期望成本不漏项、OC 曲线 α/β 精确、抽样—决策闭环 | 求稳、想拿高且可复制的队首选；模型清晰、评分点显式 |
| **C 种植策略** | 线性/整数规划 + 预测 + 风险 | **中低**（上手最快，易同质化） | 风险建模(CVaR/鲁棒)、预测严谨性、轮作约束完整 | 想快速出结果可选，但**同质严重**，国一要靠问题 3 的风险/稳健深化拉开差距 |

> **直觉**：B 模型最「标准答案化」、性价比高，适合大多数本科队；C 结果好出但易同质；A 上限最高但门槛与几何细节多。建议按「编程/几何能力 vs 稳健性偏好」选。

---

## 参考来源

- **A 题**：
  - [2024 CUMCM A题“板凳龙”全流程建模解析（几何—运动—数值一体化）](https://blog.csdn.net/weidl001/article/details/151049361)
  - [2024 高教社杯数学建模竞赛题目 A 题“板凳龙”闹元宵](https://jishuzhan.net/article/1831756251860570114) / [A 题求解过程](https://zhangjiadesanshi.blog.csdn.net/article/details/142006824)
- **B 题**：
  - [2024 B题 生产过程中的决策问题——解题思路](https://zhuanlan.zhihu.com/p/718482678)
  - [B 题解题全过程文档及程序](https://blog.csdn.net/weixin_43292788/article/details/145604523) / [国二仿真路线（GitHub）](https://github.com/BoNing-Gu/CUMCM24-ProblemB-SimulationApproach)
- **C 题**：
  - [2024 C题 农作物的种植策略详解+思路+Python 源码](https://cloud.tencent.com.cn/developer/article/2486153)
  - [基于线性规划模型的农作物种植策略（期刊）](https://xbbjb.fosu.edu.cn/zkb/CN/Y2025/V43/I6/61)

---

**待核实项汇总**（建议下载官方 PDF/附件核对后再定稿）：A 题各小问的精确时刻序列/结果文件、龙头与全龙节数、速度上下限与总时限；B 题 6 组情形的精确参数表、问 3/4 的显著性水平 α 与置信度、次品传播的工序组合逻辑；C 题土地总面积与大棚个数/类型、作物清单及各作物的亩产-成本-售价-预期销量、轮作与重茬精确规则。